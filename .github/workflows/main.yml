name: Build & Release Flutter App

on:
  push:
    branches:
      - main # Runs workflow on every push to main branch
      
permissions:
  contents: write # ✅ Required to create releases

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your repository
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ Cache dependencies for faster builds
      - name: Cache Flutter dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.pub-cache
            build
          key: ${{ runner.os }}-flutter-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-flutter-

      # 3️⃣ Setup Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.8" # ✅ Your Flutter version

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 5️⃣ Build release APK
      - name: Build Release APK
        run: flutter build apk --release

      # 6️⃣ Build release App Bundle (for Play Store)
      - name: Build Release App Bundle
        run: flutter build appbundle --release

      # 7️⃣ Upload build artifacts to GitHub Actions
      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4 # ✅ Updated to v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Upload AAB as Artifact
        uses: actions/upload-artifact@v4 # ✅ Updated to v4
        with:
          name: app-release-aab
          path: build/app/outputs/bundle/release/app-release.aab

      # 8️⃣ Create a GitHub Release (only if build succeeds)
      - name: Create GitHub Release
        if: success()
        uses: softprops/action-gh-release@v1
        with:
          name: "New Release - ${{ github.run_number }}"
          tag_name: "v${{ github.run_number }}"
          body: |
            🚀 **New Release**
            - Automatically built by GitHub Actions
            - Flutter Version: 3.32.8
            - Commit: ${{ github.sha }}
          files: |
            build/app/outputs/flutter-apk/app-release.apk
            build/app/outputs/bundle/release/app-release.aab
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
